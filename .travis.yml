language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bryant/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bryant/docker-react npm run test
